# Praxstack Cursor skill name collisions

Generated by `scripts/link-praxstack-skills.sh`. Cursor indexes one-level
directories under `.cursor/skills/<name>/SKILL.md`. When a Prax skill
name is already taken, it is installed as `prax-<name>`.

No collisions on this run.

